# claude --worktree で作る worktree へコピーする gitignored ファイル。
# worktree は fresh checkout なので、これが無いと .env が持ち込まれない。
.env

# permission の allow 設定。持ち込まないと worktree セッションで confirm が復活する
.claude/settings.local.json
