# 未実装の構想

まだ作っていないものの設計メモ。**決定でも仕様でもない。**

- 現在の振る舞いは **コードとテスト**、次に `docs/specs/`
- 決定の理由は `.claude/skills/tribunal-decisions`
- ここにあるのは「作るとしたらこういう形」という**手を動かす前の想像**

したがって:

- **ここに書いてあることを理由に実装を縛らない。** 着手する段になったら、その時点の情報で決め直す
- 実装したら、ここから消して spec / ADR 側へ移す
- 各ファイル末尾の「受け入れ条件」が、実装できたかを判断する基準

`docs/Board Game AI - Architecture Context and Design Decisions.md`（解体前の最終状態は commit `c9d2fbb`）
から移した。
