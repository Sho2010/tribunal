# PR 本文のテンプレート

`gh pr create` で PR を立てるときの本文の型。**GitHub の PR テンプレート機能（`.github/pull_request_template.md`）としては置かない**
（web から PR を作るときに人にも出てしまうため）。ここは Claude が読むための規約。

## 原則

**PR 本文は review を速くするための文書。「なぜそうしたか」は書かない。**

設計判断の理由は arch doc に書く。PR 本文の WHY は review の役に立たないので、判断の理由付けを
本文に展開しない。arch doc を更新したなら、**変更ファイルの 1 つとして扱うだけ**で、
本文から節を名指ししない（`CLAUDE.md` の「成果物に doc への参照を書かない」）。

書くのは次の 4 つだけ。

1. **何を変更したか**
2. **何ができるようになったか**
3. **注意が必要な点** — 壊れやすい / 見落としやすい / 後続タスクが踏む
4. **読むのに前提が要る点** — テクニカルな箇所、ドメイン知識が要る箇所

## 型

```markdown
## 変更

- <何を変えたか。ファイル単位ではなく変更単位で。>

## できるようになったこと

<コマンドと結果、または動くようになった経路。>

## 注意点

<無ければ節ごと省略。>

- **<箇所>** — <何に注意するか>

## 読むための前提

<非自明な挙動 / ドメイン知識。無ければ節ごと省略。>
```

節を埋めるために書くことを探さない。「注意点」「読むための前提」は**無いのが普通**。

## 書かないもの

- **WHY 全般。** 選択の理由、検討して捨てた案、トレードオフの説明
- **タスク識別子（`A1` / `B1` / `C3` …）。** branch 名と PR title にあるので本文に書かない。
  「A1 の残り」「tasks.md B2 をやった」の類は不要
- **doc の節番号 / 節名での参照**（`arch §6`、`arch doc「〜」を見る`）。本文にも title にも書かない。
  必要な事実はその場で 1 行に書き下す
- **自明な設定値**（line-length、rule set の内訳、ディレクトリ名）
- **実装の逐次説明、テストケースの列挙。** diff を読めば分かることは書かない
- **arch doc / タスク指示書に既に書かれていることの再説明**
- **タスク指示書の「判断が要るところ」の箇条書きに 1 対 1 で答えること。** あれは考慮点であって、
  本文の節立てではない

## 「注意点」に書くもの / 書かないもの

書く:

- **後続タスクが踏む前提の変更**（「`register()` が env を読むようになったので、adapter を
  import するだけのコードは env 不要になった」）
- **本番と test で挙動が変わる分岐**（「`verify_credentials=False` は test 専用。本番経路は既定の `True`」）
- **外部サービスを叩く箇所**、ネットワーク依存が入ったところ
- **冪等でない / 順序に依存する処理**

書かない:

- そう設計した理由
- 一般論としての注意喚起（「型を付けましょう」の類）
